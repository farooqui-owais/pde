VITE_API_BASE_URL=http://localhost:8000
# Google SSO (Google Cloud Console > Credentials > OAuth 2.0 Client ID, type Web).
# Authorized JavaScript origin must match the frontend origin (e.g. http://localhost:5173).
# Leave empty to hide the Google sign-in button entirely.
VITE_GOOGLE_CLIENT_ID=

