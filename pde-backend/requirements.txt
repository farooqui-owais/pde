fastapi==0.111.0
uvicorn[standard]==0.30.1
# Production WSGI/ASGI runner used by the systemd unit
# (ExecStart=/opt/pde/venv/bin/gunicorn ... -k uvicorn.workers.UvicornWorker).
# A fresh venv without this would fail with 203/EXEC on the EC2.
gunicorn==22.0.0
# 2.0.36+ fixes Python 3.13 FastIntFlag __firstlineno__ TypeError crash
sqlalchemy==2.0.36
# 2.9.10+ is required for Python 3.13 wheels (2.9.9 would try a source build
# and fail with "pg_config executable not found"); 2.9.10 still ships wheels
# for 3.11 used by pde-backend/Dockerfile.
psycopg2-binary==2.9.10
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# 4.2.x has prebuilt wheels for Python 3.13 (Jenkins); 4.0.1 would need a
# Rust toolchain. passlib 1.7.4 still works with bcrypt 4.2 (it only logs a
# harmless warning about the removed __about__ attribute).
bcrypt==4.2.1
python-multipart==0.0.9
# 2.9.2+ ships prebuilt pydantic-core wheels for Python 3.13 (used by the
# Jenkins container); 2.7.1's pydantic-core would need a Rust toolchain to
# build from source. FastAPI 0.111 allows pydantic <3, so this is compatible.
pydantic[email]==2.9.2
python-dotenv==1.0.1
alembic==1.13.1
# Exposes a /metrics endpoint (Prometheus text format) for kube-prometheus-stack
# to scrape — required by monitoring/prometheus-values.yaml's pde-backend job.
# I'm reasonably confident 7.0.0 is a real published version as of my training
# data, but pin/verify against https://pypi.org/project/prometheus-fastapi-instrumentator/
# before relying on it in case a newer version has since shipped.
prometheus-fastapi-instrumentator==7.0.0
